<UserControl x:Class="CANMonitor.Views.CanMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:prism="http://prismlibrary.com/">
    <Grid Background="White">
        <!-- 工具栏 -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Background="#F0F0F0" Padding="10" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1">
            <Button Content="开始监控" Command="{Binding StartMonitoringCommand}" Margin="5" />
            <Button Content="停止监控" Command="{Binding StopMonitoringCommand}" Margin="5" />
            <Button Content="清空列表" Command="{Binding ClearMessagesCommand}" Margin="5" />
            <Button Content="导出数据" Command="{Binding ExportDataCommand}" Margin="5" />
            <Separator Width="10" Margin="5" />
            <TextBlock Text="过滤器:" VerticalAlignment="Center" Margin="5,0,0,0" />
            <TextBox Text="{Binding FilterText}" Width="150" Margin="5" />
            <Button Content="应用" Command="{Binding ApplyFilterCommand}" Margin="5" />
            <Separator Width="10" Margin="5" />
            <CheckBox Content="自动滚动" IsChecked="{Binding AutoScroll}" VerticalAlignment="Center" Margin="5" />
        </StackPanel>
        
        <!-- 消息列表 -->
        <ListView ItemsSource="{Binding FilteredMessages}" Margin="10" VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="时间" Width="120" DisplayMemberBinding="{Binding Time}" />
                    <GridViewColumn Header="通道" Width="60" DisplayMemberBinding="{Binding Channel}" />
                    <GridViewColumn Header="ID" Width="100" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="类型" Width="60" DisplayMemberBinding="{Binding Type}" />
                    <GridViewColumn Header="数据长度" Width="80" DisplayMemberBinding="{Binding Length}" />
                    <GridViewColumn Header="数据" Width="*" DisplayMemberBinding="{Binding Data}" />
                    <GridViewColumn Header="解析" Width="*" DisplayMemberBinding="{Binding DecodedInfo}" />
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Foreground" Value="{Binding MessageColor}" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
        
        <!-- 状态栏 -->
        <DockPanel DockPanel.Dock="Bottom" Background="#F0F0F0" Height="25" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0">
            <TextBlock Margin="10,0,0,0"><Run Text="消息数量: " /><Run Text="{Binding MessageCount}" /></TextBlock>
            <TextBlock Margin="20,0,0,0"><Run Text="波特率: " /><Run Text="{Binding BaudRate}" /></TextBlock>
            <TextBlock Margin="20,0,0,0"><Run Text="状态: " /><Run Text="{Binding Status}" /></TextBlock>
        </DockPanel>
    </Grid>
</UserControl>